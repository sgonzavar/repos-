import { Text, View } from 'react-native'
import RepositoryList from './RepositoryList'

const Main = () => {
  return (
    <View style={{marginTop: 30}}>
      <RepositoryList />
    </View>
  )
}

export default Main